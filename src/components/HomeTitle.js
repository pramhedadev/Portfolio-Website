import React from "react";

function HomeTitle(props) {
  return (
    <HomeTitle
      title={props.title}
      subTitle={props.subTitle}
      text={props.text}
    />
  );
}

export default HomeTitle;
